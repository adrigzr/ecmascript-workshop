{{yield (hash onEvent=(action 'onEvent'))}}

{{#each messages as |message|}}
	<div class="alert alert-{{if (eq message.state 'passed') 'success' 'danger'}}" role="alert">{{message.title}}</div>
{{/each}}
